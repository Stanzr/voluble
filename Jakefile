var childProcess = require('child_process');
namespace( 'db', function () {
    var db = require( './db/seed.js' );
    desc( "seed db with values" );
    task( 'seed', function () {
      db.run(function(){
        console.log('done');
        process.exit(0);
        })
      } );
    } );
namespace('quality',function(){
     desc('code quality');
     task('check',function(){
            var content = [
            ]; 
  
       });
    });
